spring:
  cloud:
    config:
      name: configclient
      label: master
      discovery:
        service-id: configserver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/